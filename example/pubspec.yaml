name: shopify_pro_sdk_example
description: Production-level example app for shopify_pro_sdk.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  shopify_pro_sdk:
    path: ../
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7
  cached_network_image: ^3.3.1
  flutter_svg: ^2.0.10+1
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
